export class Persona { // Clase exportable con los datos pertenecientes a Spring con el fin de poder conectar los cambios a nuestra BD.
    id?: number; // No obligatorio.
    apellido?: string; // No obligatorio.
    nombre?: string; // No obligatorio.
    dni?: number; // No obligatorio.
}